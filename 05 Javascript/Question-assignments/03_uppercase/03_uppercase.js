// 03. Declare a varibale and assign string value to it and change all the string characters to capital letters using toUpperCase() method.

let quote = "trust the timing of your life";

console.log(quote); // In small case

// Converting to uppercase

let uppercaseQuote = quote.toUpperCase();
console.log(`String converted to uppercase (${uppercaseQuote})`);

// OR
// console.log(`${quote.toUpperCase()}`);
